CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE USER(ID BIGINT NOT NULL PRIMARY KEY,DTYPE VARCHAR(31),LOGIN VARCHAR(255),PASSWORD VARCHAR(255),ARTICLE_ID_ARTICLE BIGINT,FAMILYNAME VARCHAR(255),FIRSTNAME VARCHAR(255),REFERENCECHERCHEUR INTEGER)
CREATE MEMORY TABLE ARTICLE(ID_ARTICLE BIGINT NOT NULL PRIMARY KEY,CONTENU VARCHAR(255),IMAGE VARCHAR(255),REPOIMAGE VARCHAR(255),TITRE VARCHAR(255))
CREATE MEMORY TABLE SEQUENCE(SEQ_NAME VARCHAR(50) NOT NULL PRIMARY KEY,SEQ_COUNT NUMERIC(38))
ALTER TABLE USER ADD CONSTRAINT FK_USER_ARTICLE_ID_ARTICLE FOREIGN KEY(ARTICLE_ID_ARTICLE) REFERENCES ARTICLE(ID_ARTICLE)
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 10
SET SCHEMA PUBLIC
INSERT INTO SEQUENCE VALUES('SEQ_GEN',0)
